#include <stdio.h>
main()
{
float x, y;
printf("Enter a number to be inverted : ");
scanf("%f", &x);
if (x==0)
printf("wrong");
else
{y = 1/x;
printf("The inverse of %f equals %f\n", x, y);}
getch();
}
